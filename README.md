# GraphQL Middleware

Authorization & rate limiting for GraphQL. Protect Ferrow GraphQL endpoints.

```javascript
const middleware = new GraphQLMiddleware(rules);
app.use(middleware);
```

## Features
- ✓ Field-level auth
- ✓ Rate limiting
- ✓ Query complexity analysis
- ✓ Ferrow GraphQL agents

## License: MIT
## Examples
