{
  "name": "@ferrow/graphql-middleware",
  "version": "2.0.0",
  "description": "GraphQL operation guard for a supplied AST: fragment-aware depth limit with cycle guard, per-field complexity scoring, operation allowlist/denylist, and introspection blocking. graphql-js is optional — accepts its document shape structurally, zero hard dependencies.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "demo": "tsc && node dist/examples/demo.js"
  },
  "keywords": [
    "graphql",
    "security",
    "depth-limit",
    "query-complexity",
    "typescript",
    "middleware"
  ],
  "author": "Ruzylo-cloud",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5"
  }
}
